<h1>Posts</h1>
<%= link_to 'New Post', new_post_path%>

<% if @posts.present? %>
<ul>
  <% @posts.each do |post|%>
    <li> <%= post.title%> 
      <%= link_to 'Show Post', post_path(post)%>
      <%= link_to 'Edit Post', edit_post_path(post)%>
      <%= link_to 'Delete Post', post_path(post), method: :delete, data: { confirm: 'Are u sure?' }  %>
    </li>
  <% end %>
</ul>
<% else %>
  <h3>No Posts</h3>
<% end %>